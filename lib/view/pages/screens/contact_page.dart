import 'package:easy_stepper/easy_stepper.dart';

class ContancePage extends StatelessWidget {
  const ContancePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
